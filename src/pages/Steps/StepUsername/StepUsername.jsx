import React from 'react'

const StepUsername = ({onNext}) => {
  return (
    <>
    <div>This is the username step</div>
    <button onClick={onNext}>Next</button>
  </> 
  )
}

export default StepUsername